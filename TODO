High Priority:
TEST: return type string doesn't match stored value
TEST: input parameter name string doesn't match stored value
TEST: input parameter type string doesn't match stored value

TEST: too many output parameters
TEST: multiple output parameters
TEST: output parameter name doesn't exist
TEST: output parameter name wrong index
TEST: output parameter index is wrong, output parameters in the wrong order
TEST: output parameter doesn't exist
TEST: output parameter type not found by ID lookup
TEST: output parameter type hash doesn't match stored value
TEST: output parameter type string doesn't match stored value
TEST: output parameter name not found by ID lookup
TEST: output parameter name hash doesn't match stored value
TEST: output parameter name string doesn't match stored value

TEST: input parameter used as an output parameter
TEST: output parameter used as an input parameter

TEST: build function definition with input & output parameters in mixed order, not 2 clear blocks

TEST: end function not all input parameters have been stored
TEST: end function not all output parameters have been stored

MACROS: TEST macros will crash if run off end of input or output arrays - need to handle case not just print message

Medium Priority:
LES: function definition need input & output param array - or single param array?
MACROS: have generic ADD_INPUT, ADD_OUTPUT without index parameter (auto-computed)
MACROS: WORK OUT HOW TO DO THE RETURN VALUE IN MACROS
LES: tests - need tests for each fail case that is tested for
LES: need to add fake output
LES: tests - test input/output parameter is a ptr (to a structure)

Low Priority:
LES: how will output parameters work? Do they need to be sent to the server (yes i.e. in/out params)
MACROS: WORK OUT HOW TO DO THE ERROR HANDLING 
LES: need common error handling & log system
LES: need to list the data storage that is required by the global data
TEST: error handling run out of memory when adding new function or new string e.g. array limit reached
LES: memory management for input & output parameter blocks per function
LES: hash table with binary search
LES: need good debugging ability for the packets before sent and at server end
LES: send packet data to server?
LES: server send global test data instead of building it internally?
